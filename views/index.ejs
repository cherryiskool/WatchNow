
<h1>Popular</h1>
<!-- iterate over the all the files sent and create a thumbnail for each video which links to the actual video -->

<div class="videoPanorama">
    <% for(let i = 0;i < 3; i++) { %>
        <div class="videoCard">
            <a href="/videos/watch/<%= popularVideos[i].fileName %>">
                <video height="400" width="400" class="videoThumbnail">
                    <source src="/videos/<%= popularVideos[i].fileName %>">
                </video>
            </a>
            <p><%= popularVideos[i].title %></p>
            <img src="/profilePictures/<%= popularVideos[i].pfp %>" class="pfpUnderVideo" ></img>
            <p><%= popularVideos[i].username %></p>
            <p>Views <%= popularVideos[i].views %></p>
        </div>
    <% } %>
</div>

<h1>Recommended</h1>

<div class="videoPanorama">
    <% if(subbedVideos) { %>
        <% for(let i = 0; i < 3; i++) { %>
            <div class="videoCard">
                <a href="/videos/watch/<%= subbedVideos[i].fileName %>">
                    <video height="400" width="400" class="videoThumbnail">
                        <source src="/videos/<%= subbedVideos[i].fileName %>">
                    </video>
                </a>
                <p><%= subbedVideos[i].title %></p>
                <p><%= subbedVideos[i].username %></p>
                <p>Views <%= subbedVideos[i].views %></p>
            </div>
        <% } %>
    <% } %>
</div>

<h1>New Uploads</h1>

<div class="videoPanorama">
    <% for(let i = 0; i < 3; i++) { %>
        <div class="videoCard">
            <a href="/videos/watch/<%= newestVideos[i].fileName %>">
                <video height="400" width="400" class="videoThumbnail">
                    <source src="/videos/<%= newestVideos[i].fileName %>">
                </video>
            </a>
            <p><%= newestVideos[i].title %></p>
            <p><%= newestVideos[i].username %></p>
            <p>Views <%= newestVideos[i].views %></p>
        </div>
    <% } %>
</div>